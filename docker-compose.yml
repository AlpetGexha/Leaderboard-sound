services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
      - "5173:3000"
    env_file:
      - .env
    volumes:
      - ticket_arena_data:/app/data
    restart: unless-stopped

volumes:
  ticket_arena_data:
